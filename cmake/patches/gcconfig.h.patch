*** gc-7.2/include/private/gcconfig.h	2012-05-11 19:09:00.000000000 +0200
--- gc-7.2/include/private/gcconfig.h.qnx	2013-07-25 10:17:15.019087900 +0200
***************
*** 43,48 ****
--- 43,72 ----
  #   define LINUX
  # endif
  
+ /* for BB10 support */
+ # if defined(__QNX__)
+ #  if defined(__arm__)
+ #    define ARM32
+      extern int __data_start[];
+ #    define DATASTART ((ptr_t)__data_start)
+      extern char _end[];
+ #    define DATAEND ((ptr_t)(&_end))
+ #  else
+ /* for simulator */
+ #    define I386
+      extern char etext[];
+ #    define DATASTART ((ptr_t)(etext))
+      extern int _end[];
+ #    define DATAEND (_end)
+ #  endif
+ /* common things */
+ #  define QNX
+ #  define OS_TYPE "QNX"
+ #  define SA_RESTART 0
+ #  define HEURISTIC1
+ #  define mach_type_known
+ # endif
+ 
  /* And one for NetBSD: */
  # if defined(__NetBSD__)
  #    define NETBSD
***************
*** 73,79 ****
  # if defined(__arm) || defined(__arm__) || defined(__thumb__)
  #    define ARM32
  #    if !defined(LINUX) && !defined(NETBSD) && !defined(OPENBSD) \
!         && !defined(DARWIN) && !defined(_WIN32) && !defined(__CEGCC__)
  #      define NOSYS
  #      define mach_type_known
  #    endif
--- 98,105 ----
  # if defined(__arm) || defined(__arm__) || defined(__thumb__)
  #    define ARM32
  #    if !defined(LINUX) && !defined(NETBSD) && !defined(OPENBSD) \
!         && !defined(DARWIN) && !defined(_WIN32) && !defined(__CEGCC__) \
!         && !defined(QNX)
  #      define NOSYS
  #      define mach_type_known
  #    endif
