;; -*- mode: scheme; coding: utf-8; -*-
;; math.scm: mathmatic library.
#!compatible
(library (math)
    (export :all)
    (import (math random)
	    (math hash)
	    (math prime)
	    (math helper))
  )