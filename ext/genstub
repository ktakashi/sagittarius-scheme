;; -*- scheme -*-
(add-load-path "../sitelib")
(add-load-path "../lib")

(import (sagittarius cgen))
(when (not (= (length (command-line)) 2))
  (error 'genstub "invalid command-line arguments" (command-line)))
(let ((file (cadr (command-line))))
  (cgen file))